<% content_for :page_banner do %>
  <h1>Rent <%= @locker %></h1>
<% end %>


<%= simple_form_for [:admin, @locker] do |f| %>
  <%= render 'suite_fields', :f => f %>

  <%= f.submit 'Save changes', :class => 'btn btn-primary' %>
  <%= f.cancel %>
<% end %>
